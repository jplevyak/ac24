#include <iostream>
#include <fstream>
#include <string>

using namespace std;

int main() {
  string line;
  ifstring ifs("1");

  while (getline(ifs1, line)) {
    cout << line << endl;
  }
  return 0;
}
